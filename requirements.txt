# Core data
pandas>=2.0
polars>=0.20
numpy>=1.24
pyarrow>=14.0          # parquet I/O

# Candidate generation / collaborative filtering
implicit>=0.7          # ALS, item-kNN
gensim>=4.3            # item2vec

# Ranking
lightgbm>=4.0
scikit-learn>=1.3

# Embeddings
torch>=2.0
torchvision>=0.15      # ResNet-50 image embeddings
sentence-transformers>=2.2  # text embeddings

# Viz + utilities
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65

# Kaggle data download / submission
kaggle>=1.6

# Note: regenerate exact pins from your working environment with
#   pip freeze > requirements.txt
# to guarantee reproducibility.
